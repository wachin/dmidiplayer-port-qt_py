#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$ROOT/drumstick:$ROOT/dmidiplayer${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m dmidiplayer_py "$@"
